############################################################
# CourseHelper Docker Environment Variables (.env.example)
# Copy to .env and replace placeholder secrets before running.
############################################################

# === Core AI Key (used by backend & python) ===
GEMINI_API_KEY=REPLACE_WITH_YOUR_GEMINI_KEY

# === Backend → Python integration ===
# Internal URL for python-service when running in compose.
API_BASE_URL=http://python-service:8000

# === AI Model Configuration (override defaults if needed) ===
API_MODEL_PROVIDER=gemini
API_MODEL_NAME=gemini-2.0-flash

# === JWT Configuration ===
# Provide a strong secret (not the example) in production.
JWT_SECRET=CHANGE_ME_SUPER_SECRET
JWT_EXPIRATION=86400000

# === Python Service Tunables ===
# Timeout (seconds) for Gemini file upload operations.
GOOGLE_GENAI_UPLOAD_TIMEOUT=1000

# === Service Hosts (override only for non-compose dev) ===
REDIS_HOST=redis
REDIS_PORT=6379
QDRANT_HOST=qdrant
QDRANT_PORT=6333
